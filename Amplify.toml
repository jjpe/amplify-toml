use_reporting = false # TODO: deprecated
report_address = "tcp://127.0.0.1:9090" # TODO: deprecated
quiet = false

[spoofax_server]
language_directory = "./languages/"
receive_address = "tcp://127.0.0.1:6001"
send_address = "tcp://127.0.0.1:6000"
pretty_serialize = true

[broadcaster]
receive_address = "tcp://127.0.0.1:6000"
broadcast_address = "tcp://127.0.0.1:6001"
num_msgs = "loop"

[collector]
receive_address = "tcp://127.0.0.1:6001"
mongodb_url = "mongodb://localhost:27017"
header_period = 50 # valid values: a nonnegative integral number
num_msgs = "30" # valid values: either "loop" or "N" where N is a nonzero integral number

[client]
receive_address = "tcp://127.0.0.1:6001"
send_address = "tcp://127.0.0.1:6000"
num_msgs = "1000"
